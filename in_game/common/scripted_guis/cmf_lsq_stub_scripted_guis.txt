### CMF: Stub scripted GUIs for LSQ (Location Specialization QoL)
# These no-op scripted GUIs are overridden by LSQ's real implementations when LSQ is loaded.
# They exist so that GlorpUI's on_action calls to LSQ scripted GUIs don't error when LSQ is absent.

lsq_check_for_auto_expand_when_upgrading_building = {
	effect = { }
}

lsq_check_for_auto_expand_when_removing_building = {
	effect = { }
}
