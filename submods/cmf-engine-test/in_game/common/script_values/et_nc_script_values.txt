# Test 84: reads a variable off a country the GUI names directly, with no player
# involved, via GuiScope.AddScope('country', GetCountry('CAS').MakeScope).ScriptValue.
# et_nc_seed sets the source variable to 42.

et_nc_sv = {
	value = 0
	if = {
		limit = {
			scope:country = { has_variable = et_nc_probe_value }
		}
		add = scope:country.var:et_nc_probe_value
	}
}
