# List _on_changed callbacks - required for all list settings.
# Each must call cmm_apply_list_change with the correct composite setting key.

cmf_feature_test__ordered_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__ordered_list
		}
	}
}

cmf_feature_test__unordered_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__unordered_list
		}
	}
}

cmf_feature_test__dynamic_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__dynamic_list
		}
	}
}

cmf_feature_test__from_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__from_list
		}
	}
}

cmf_feature_test__order_test_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__order_test_list
		}
	}
}

cmf_feature_test__global_ordered_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__global_ordered_list
		}
	}
}

cmf_feature_test__global_dynamic_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__global_dynamic_list
		}
	}
}

cmf_feature_test__global_from_list_on_changed = {
	scope = country

	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__global_from_list
		}
	}
}

# Conditional visibility - only shown when the toggle is enabled.
cmf_feature_test__conditional_numeric_on_changed = {
	scope = country
	is_shown = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

# Conditional enabled state - control is greyed out and disabled while the toggle is off.
cmf_feature_test__conditional_enabled_on_changed = {
	scope = country
	is_valid = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

cmf_feature_test__conditional_enabled_text_on_changed = {
	scope = country
	is_valid = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

cmf_feature_test__conditional_enabled_list_on_changed = {
	scope = country
	is_valid = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = {
		cmm_apply_list_change = {
			setting = cmf_feature_test__conditional_enabled_list
		}
	}
}

# Cascade test - hidden when Toggle is off so the owning group and tab can empty and hide.
cmf_feature_test__cascade_group_demo_on_changed = {
	scope = country
	is_shown = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

cmf_feature_test__cascade_tab_demo_on_changed = {
	scope = country
	is_shown = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

# Empty Demo mod - all settings hidden when Toggle is off, so the whole mod leaves the list.
cmf_empty_demo__demo_numeric_on_changed = {
	scope = country
	is_shown = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}

cmf_empty_demo__demo_bool_on_changed = {
	scope = country
	is_shown = {
		"variable_map(cmm|flag:cmf_feature_test__toggle)" >= 1
	}
	effect = { }
}
